{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main window permissions for the native editable HWPX prototype",
  "windows": ["main"],
  "permissions": ["core:default"]
}
