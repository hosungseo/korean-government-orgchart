{
  "name": "korean-government-orgchart-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Windows desktop wrapper for the Korean government organization-chart generator",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:offline": "tauri build --config src-tauri/tauri.offline.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
